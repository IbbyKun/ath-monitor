node_modules
dist
.vscode
.cursor
.claude
vendor
.DS_Store
.env
.env.*
.env.local
.env.production
# ...but templates carry no secrets and must be committed.
!.env.docker.example
!.env.example
*.pem
*.key
*.log
*-audit.json
audit.json
package-lock.json

# Locally generated shared secrets (JWT/crypto keys reused across services)
.secrets/

# Runtime log dirs the logger creates from an absolute Windows path when
# NODE_ENV/log paths are misconfigured — never meant to be committed.
Backend/**/D:\\*
.audit.json